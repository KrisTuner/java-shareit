# 🛋️ ShareIt - Сервис шеринга вещей

![Java](https://img.shields.io/badge/Java-21-orange)
![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.3.2-brightgreen)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue)
![Maven](https://img.shields.io/badge/Maven-3.9-red)
![JUnit](https://img.shields.io/badge/JUnit-5-blue)
![Docker](https://img.shields.io/badge/Docker-✓-blue)

---

## 📝 Описание проекта
ShareIt — это backend-приложение для платформы шеринга вещей, где пользователи могут:

*   **Делиться** своими вещами с другими пользователями.
*   **Бронировать** вещи на определённые даты.
*   **Оставлять запросы** на нужные вещи, которых нет в данный момент.
*   **Писать комментарии** после использования вещей.

Проект представляет собой многомодульное Spring-приложение с разделением на **API Gateway** и **основный сервер**, что обеспечивает:

*   Чёткое разделение ответственности.
*   Улучшенную безопасность (валидация входных данных на уровне gateway).
*   Масштабируемость архитектуры.

### Цели разработки
*   Создание надёжного REST API для сервиса шеринга.
*   Реализация сложной бизнес-логики бронирований и запросов.
*   Обеспечение целостности данных на уровне БД.
*   Покрытие кода тестами (unit + интеграционные).
*   Следование лучшим практикам Java-разработки и чистой архитектуре.

---

## 🛠 Стек технологий

### Backend
*   **Java 21** — современная версия с новыми возможностями языка.
*   **Spring Boot 3.3.2** — основа приложения.
*   **Spring Web** — создание REST API.
*   **Spring Data JPA** — работа с базой данных.
*   **Spring Validation** — валидация данных.
*   **Spring Actuator** — мониторинг и метрики приложения.
*   **Maven** — сборка и управление зависимостями.

### Базы данных
*   **PostgreSQL 16** — основная БД для production-среды.
*   **H2 Database** — in-memory БД для тестирования.

### Тестирование
*   **JUnit 5** — модульные тесты.
*   **MockMvc** — тестирование контроллеров.
*   **@DataJpaTest** — тестирование JPA-слоя и репозиториев.
*   **@JsonTest** — тестирование сериализации/десериализации DTO.

### Инфраструктура
*   **Docker** — контейнеризация приложения.
*   **REST Client (Apache HTTP Client)** — надёжная коммуникация между модулями.
*   **Lombok** — уменьшение boilerplate-кода.

---

## 🏗 Архитектура проекта

Проект разделён на два модуля:

### 1. Gateway (API Gateway) — порт `8080`
*   Принимает все входящие HTTP-запросы от клиентов.
*   Проводит первичную валидацию данных (форматы, обязательные поля).
*   Перенаправляет валидные запросы на сервер через REST-клиенты.
*   Возвращает ответы от сервера клиентам.

### 2. Server (Основной сервер) — порт `9090`
*   Содержит всю бизнес-логику приложения.
*   Работает с базой данных через JPA-репозитории.
*   Обрабатывает сложные бизнес-правила (доступ к бронированиям, статусы вещей).
*   Выполняет сложные запросы к БД для формирования сводной информации (например, история бронирований с пагинацией).

---

## 🚀 Инструкция по развёртыванию и системные требования

### Системные требования
*   **Java Development Kit (JDK):** 21 или выше.
*   **Maven:** 3.9 или выше.
*   **Docker & Docker Compose:** (Рекомендуется) для запуска PostgreSQL.
*   **PostgreSQL:** 16 (если запускаете БД не в Docker).
*   **Операционная система:** Кроссплатформенно (Windows, Linux, macOS).

### Запуск проекта (Docker Compose)

Самый простой способ запустить проект — использовать Docker Compose. В корне проекта должен находиться файл `docker-compose.yaml`.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <URL вашего репозитория>
    cd java-shareit
    ```

2.  **Соберите проект с помощью Maven:**
    ```bash
    mvn clean package
    ```

3.  **Запустите все сервисы (PostgreSQL, Gateway, Server) с помощью Docker Compose:**
    ```bash
    docker-compose up -d
    ```

4.  **Приложение будет доступно по адресу:** `http://localhost:8080`.

### Запуск проекта (Вручную)

Если вы не хотите использовать Docker, вы можете запустить приложение вручную.

1.  **Запустите PostgreSQL**, убедитесь, что база данных `shareit` создана, и пользователь `shareit` с паролем `shareit` имеет к ней доступ.

2.  **Соберите проект:**
    ```bash
    mvn clean package
    ```

3.  **Запустите сервер (из папки `server`):**
    ```bash
    cd server
    java -jar target/server-0.0.1-SNAPSHOT.jar
    ```
    Сервер запустится на порту `9090`.

4.  **Запустите gateway (из папки `gateway` в новой консоли):**
    ```bash
    cd gateway
    java -jar target/gateway-0.0.1-SNAPSHOT.jar
    ```
    Gateway запустится на порту `8080`.

5.  **Приложение будет доступно по адресу:** `http://localhost:8080`.

---

## 📊 Выводы и результаты

В результате выполнения проекта был разработан полностью функционирующий backend для сервиса шеринга вещей. Ключевые выводы и достижения:

1.  **Реализована многоуровневая архитектура:** Разделение на Gateway и Server позволило четко разграничить зоны ответственности. Gateway занимается валидацией и маршрутизацией, а Server — исключительно бизнес-логикой и работой с данными. Это повышает безопасность и упрощает потенциальное масштабирование.

2.  **Создана сложная бизнес-логика:** Успешно реализованы все ключевые сценарии использования:
    *   Создание и управление бронированиями с различными статусами (WAITING, APPROVED, REJECTED).
    *   Поиск вещей по названию и описанию.
    *   Функционал запросов вещей (Item Requests), позволяющий пользователям инициировать появление нужных им предметов.
    *   Система комментариев, доступная только после завершения бронирования, что гарантирует достоверность отзывов.

3.  **Обеспечена целостность данных:** Использование Spring Data JPA и правильно спроектированной схемы базы данных (связи между таблицами, внешние ключи, уникальные ограничения) гарантирует консистентность и надежность хранимой информации.

4.  **Высокое качество кода:**
    *   Код покрыт модульными и интеграционными тестами (JUnit, MockMvc, @DataJpaTest), что подтверждает его работоспособность и упрощает дальнейшее развитие.
    *   Применены современные практики Java-разработки: использование Lombok для уменьшения шаблонного кода, мапперы для преобразования сущностей в DTO, централизованная обработка ошибок.
    *   Следование принципам REST API сделало интерфейс приложения интуитивно понятным.

5.  **Готовность к контейнеризации:** Проект полностью готов к запуску в Docker-среде, что упрощает его развертывание и интеграцию в современные CI/CD пайплайны.
